{
  "name": "todo",
  "version": "1.0.0",
  "description": "A simple TODO list app",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "npm run build:client",
    "build:client": "browserify ui/main.js --out public/bundle.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Alan Shaw",
  "license": "MIT",
  "dependencies": {
    "hapi": "^16.5.0",
    "react": "^15.6.1"
  }
}
